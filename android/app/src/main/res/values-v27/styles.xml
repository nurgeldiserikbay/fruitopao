<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Отдельный файл для API 27+: там появился режим выреза. Просим систему
         разложить окно под вырез, чтобы WebView доходил до краёв экрана и
         полос по бокам не оставалось вовсе. На более старых версиях этот
         атрибут не существует, поэтому он и вынесен в values-v27. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@drawable/window_background</item>
        <item name="android:statusBarColor">@color/sceneEdge</item>
        <item name="android:navigationBarColor">@color/sceneEdge</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
