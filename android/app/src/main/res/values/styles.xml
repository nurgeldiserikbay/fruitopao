<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- Фон окна активности. Без него тема AppCompat.Light отдаёт белый, и
             в ландшафте по краям экрана видны белые полосы: WebView не
             доходит до краёв, а под ним светится окно. Цвет совпадает с
             кромкой сцены, поэтому шва не видно даже там, где WebView не
             дотянулся. -->
        <item name="android:windowBackground">@drawable/window_background</item>
        <item name="android:statusBarColor">@color/sceneEdge</item>
        <item name="android:navigationBarColor">@color/sceneEdge</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>