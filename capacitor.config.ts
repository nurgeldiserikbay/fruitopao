import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thelightcome.fruitopao',
  appName: 'Fruito Pao',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  }
};

export default config;
